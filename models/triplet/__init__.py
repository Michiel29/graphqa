from .dist_mult import ConcatDot, DistMult, DistMultEntityOnly, RotatE

triplet_dict = {
    'concat_dot': ConcatDot,
    'distmult': DistMult,
    'distmult_entity_only': DistMultEntityOnly,
    'rotate': RotatE,
}
